{
  "name": "@tana/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Tana CLI - Command-line tools for developers and users",
  "bin": {
    "tana": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --minify",
    "test": "bun test",
    "link": "bun link"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "prompts": "^2.4.2",
    "@noble/ed25519": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/prompts": "^2.4.9"
  }
}
