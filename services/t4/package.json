{
  "name": "@tananetwork/t4",
  "version": "0.1.8",
  "type": "module",
  "description": "Content-addressable storage server for Tana blockchain",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile ../../dist/tana-t4",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
