{
  "name": "@tananetwork/mesh",
  "version": "0.1.7",
  "type": "module",
  "description": "Network discovery and coordination service for Tana validators",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile ../../dist/tana-mesh",
    "test": "bun test"
  },
  "dependencies": {
    "@tananetwork/crypto": "^0.1.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
